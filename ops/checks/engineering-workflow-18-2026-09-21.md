# Engineering workflow kit and publication — checkpoint 18

User requested Git publication plus governed evolution of requirements/design/
acceptance, reusable skills/prompts/templates and task/subtask model orchestration.
Resumed 2026-09-21; no product implementation or gate promotion in this increment.
This receipt is revision-bound by the Git commit containing it. Remote equality
is verified after push, not asserted in advance here.

## Saved and checked

- Four repository-local skills: design, implementation, review and orchestration.
  Skill-creator guidance kept these scoped and reusable, with shared supporting
  resources rather than duplicated full instructions. All four quick validations
  passed; this is format validation, not independent behavioral certification.
- Four prompts, five templates, ways-of-working and change-control guides, plus a
  real-change register initially empty (no invented approved change records).
- Read-only task routing preserves 132 parent assignments and verifies 660 task/
  role combinations. Terra High default, Sol High design/review, Sol XHigh high-risk
  semantics, Luna Medium only for frozen mechanical leaves. Availability/current
  harness still governs actual selection; no automatic launcher or scheduler.
- Change validation rejects missing impact, lost obligation mapping, templates
  registered as records, self/stale approval and goal changes without owner decision.
  It checks structure/references, not reviewer authenticity or complete semantics.
- AGENTS, README, handoff and npm check integration updated. Requirements may evolve
  with documented evidence/impact and review; old criteria/history remain traceable,
  affected artifacts/roadmaps/tests update together and stale reviews are reopened.

## Actual verification

[Raw checks](engineering-workflow-18-check-results.json): `npm run check` passed
44 tests (36 existing plus eight tooling tests), zero failures/skips. Specification
self-tests passed with 480 original vectors / 2,608 assertions / 132 tasks covered.
`node ops/check-spec.mjs --self-test --verify-sources` passed all 27 pinned source
hashes. `check:engineering` verified 4 skills, 4 prompts, 5 templates and 660 routes.
The 480 comparator positives and 3,088 negatives remain synthetic definition tests.

[Publication/preservation checks](engineering-workflow-18-publication-checks.json)
confirm 49 prior runtime/UI/test/vector/canonical-plan/model-policy files unchanged.
Package scripts and new tooling/tests are deliberate changes. All original 132
tasks remain planned and 480 criteria not_run; approved implementation packets zero.
No real enterprise acceptance, workflow execution or independent review was added.

Focused exact staged-byte scan found no credential-pattern hits/forbidden data
paths in 334 files before this receipt and its publication-check file were added.
The scan initially hit sandbox Git-subprocess denial; approved rerun passed. This
is not exhaustive security certification. Final staged scan/whitespace checks are
repeated before commit. Runtime/customer data and logs remain excluded.

## Scope and history

This publication includes the saved, previously unpushed checkpoints 14–17; their
receipts remain historical rather than rewritten to claim prior publication.
Original gate totals remain P 4/12, E 0/16 and WF 0/16. Production-ready remains
false. A public source repository is not a hosted enterprise installation.

RM-WOW-18 used one bounded official-documentation mission; total 2/5 used, three
remaining. Its [receipt](engineering-workflow-research-18.md) records source order
and recommendations. No global skill/config install, model change, delegated run,
paid fallback, live external product effect or deployment was performed.

Next bounded product work remains foundation packet integration/review (T-01/T-02),
with full schemas, seed data and real observation adapters. These workflow assets
do not approve those packets or start broad implementation.
