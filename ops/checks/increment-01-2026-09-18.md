# Increment 01 verification receipt

- Checked at: `2026-09-18T15:04:34Z`
- Product revision: `tree-sha256:ec94619dde2a34da4b37c3c786ab074d75b84c7e96a5550e28750f8384219abc`
- Revision method: SHA-256 of the sorted `sha256sum` output for `server.mjs`, `package.json`, and all files under `src/`, `public/`, and `tests/`.
- Research missions started: `0` (existing research reused)
- Command: `npm run check`
- Result: **PASS — 9 passed, 0 failed, 0 skipped**
- Live smoke check: loopback server started at `127.0.0.1:4310`; `GET /api/health` returned `{"status":"ok","product":"OrgWard Enterprise Studio","increment":1}`; listener was then stopped.

## Passing checks

1. Map filters types while retaining a valid linked subgraph.
2. Selection context resolves linked objects and remains view-independent.
3. Map zoom is centred and bounded.
4. Guided chat saves a scoped brief with assumptions and unknowns.
5. Generated blueprint covers every contracted area and retains epistemic metadata.
6. Integrity checks catch missing areas, dangling links, and incomplete ownership.
7. HTTP workflow persists chat, brief, blueprint, and map across restart.
8. Projects remain isolated and the served UI exposes the interactive-map workflow and security headers.
9. API rejects malformed project identifiers and empty chat messages.

## Gate evidence

### P-01 — verified

The API integration test creates a private project, submits all four guided answers, and asserts a saved brief, assumptions, unknowns, conversation history, and audit event. The server binds to loopback by default and stores projects separately.

### P-02 — verified

The model test asserts one saved blueprint version covers all 10 contracted design areas (the release wording groups capabilities with processes and people/agents with responsibility/authority), with 25+ objects, 30+ typed links, proposed-design status, confidence, and provenance.

### P-03 — verified

The positive blueprint passes structural and reference validation. The negative test removes a required area and owner and introduces a dangling reference; it asserts the corresponding error and actionable gap results.

### P-04 — verified

The served UI includes graph/list views, type filters, object selection/detail, and zoom controls. Shared interaction logic used by that UI is tested for filtering, linked selection context, graph/list selection continuity, and bounded centred zoom. The API workflow asserts map content includes capability, process, role, and system nodes plus typed links.

## Boundary and non-passes

P-05 through P-12 remain pending. Restart persistence and project isolation are verified for this slice, but P-10 does not pass because later instructions, task runs, and artifacts do not yet exist. P-12 does not pass because the complete release workflow extends beyond this increment. No browser screenshot test was available in this workspace; the map pass is backed by served-UI, shared-interaction, syntax, and live HTTP checks rather than a screenshot comparison.
