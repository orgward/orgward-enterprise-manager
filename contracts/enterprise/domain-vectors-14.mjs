// Definition-only observations, not real execution or independent approval.
// Expand aliases into full reviewed seeds; collect observations from actual systems.
const eq=(path,value)=>({path,op:'eq',value});
const lte=(path,value)=>({path,op:'lte',value});
const v=(task,ac,name,initial,steps,assertions)=>({id:`DV-${task}-AC${ac}`,taskId:task,acceptanceId:`${task}-AC${ac}`,name,initial,steps,assertions,status:'not_run',reviewStatus:'pending',reviewer:null,testPath:`tests/acceptance/${task.toLowerCase()}.test.mjs`});
export const domainVectors=[
  v('T-90',1,'Context binds enterprise meaning integrity and explicit coverage gaps',
    {change:'change-contact',system:'contact-service',baseline:'b7',snapshot:'sentinel-s7',policy:'p3',property:'Party.email',unknown:'optional-legacy-analytics',excluded:'other-entity-payroll',criticalUnknowns:[]},
    ['Select contact-service from its Studio system perspective and pin context from the accepted b7 baseline and its matching Sentinel compilation under authorized retrieval.','Independently resolve the manifest bytes, source revisions, property authority, requirements, risks and coverage exclusions; reopen the SDLC case and return to the same Studio object.'],
    [eq('/manifest/binding',{baseline:'b7',snapshot:'sentinel-s7',policy:'p3'}),eq('/manifest/missingMandatoryFacets',[]),eq('/coverage/unknownIds',['optional-legacy-analytics']),eq('/coverage/excludedIds',['other-entity-payroll']),eq('/navigation/canonicalSystem','contact-service')]),
  v('T-90',2,'Missing stale denied or truncated mandatory context blocks planning',
    {cases:['critical-authority-missing','snapshot-built-from-b6','retrieval-denied','token-limit-drops-guardrail'],canary:'RESTRICTED_CONTEXT_CANARY',guardrail:'approved-property-source-only'},
    ['Run each context fault from a fresh seed and attempt plan creation using both the case UI and direct command, including a model budget too small for mandatory constraints.','Inspect a scoped actionable gap, no runnable plan, retained draft and independently captured retrieval/log responses; ensure neither hidden field names nor protected bytes leak.'],
    [eq('/cases/executed',4),eq('/planning/runnablePlans',0),eq('/guardrails/silentlyOmitted',[]),eq('/gaps/missingRecoveryAction',[]),eq('/leaks/canaryHits',0)]),
  v('T-90',3,'Upstream publication invalidates current use without rewriting pinned history',
    {cases:['policy-p3-to-p4','Party.email-authority-a-to-b'],oldContext:'ctx7',consumers:['requirement-r1','eval-e1','approval-a1'],barrier:'publication-commit-before-projection-refresh'},
    ['Publish each reviewed upstream change separately while an old-context release waits at the broker and hold projection consumers behind the commit barrier.','Attempt dispatch with approval-a1, inspect authoritative invalidations and reconstruct the old manifest from retained bytes; create a successor context instead of changing ctx7.'],
    [eq('/stale/consumerIds',['approval-a1','eval-e1','requirement-r1']),eq('/oldApproval/newContextDispatches',0),eq('/history/ctx7BytesChanged',false),eq('/successor/distinctContext',true),eq('/projectionLag/treatedAsFresh',false)]),
  v('T-90',4,'Runtime discovery proposes correction without legitimizing observed bypass',
    {trace:'read-trace-a',observedSource:'replica-b',acceptedAuthority:'identity-a',property:'Party.email',baseline:'b7'},
    ['Ingest actual isolated runtime evidence that contact-service reads replica-b and link the observed read to the existing property and release.','Inspect the proposed claim/correction and attempt treating observed popularity as accepted authority; independently review a correction only through the normal change protocol.'],
    [eq('/observation/source','replica-b'),eq('/claim/state','proposed'),eq('/accepted/authority','identity-a'),eq('/observation/capabilitiesIssued',0),eq('/correction/sourceTrace','read-trace-a')]),
  v('T-91',1,'Evaluation tests business behavior with independent fault and recovery controls',
    {requirement:'r-contact-authority',outcome:'authorized-contact-onboarding',scope:'LE:A',risk:'wrong-source',source:'b7/Party.email',cases:['approved-source-success','replica-read-denied','source-outage-recovered'],evaluator:'independent-reviewer'},
    ['Derive and independently review typed inputs, output expectations and a source-observation assertion for all three cases, pinning the requirement and context versions.','Run the implemented behavior against isolated source fixtures and inspect the trace to outcome/scope/risk/source; use a broken implementation that only prints the requirement text as the negative control.'],
    [eq('/trace/missingRequiredLinks',[]),eq('/behavior/casesExecuted',3),eq('/behavior/violations',0),eq('/textOnlyControl/accepted',false),eq('/evaluation/generatorSelfApproval',false)]),
  v('T-91',2,'Orphan code deleted tests and uncovered guardrails cannot pass assurance',
    {cases:['unmapped-new-code','mandatory-egress-guardrail-uncovered','delete-failing-test'],acceptedSuite:'suite-v2',candidate:'candidate-a'},
    ['Submit each altered candidate independently against the accepted test and trace inventory rather than discovering required tests only from the candidate checkout.','Inspect failed assurance and the exact missing obligation, including evidence that the candidate cannot rewrite its own mandatory suite or acceptance denominator.'],
    [eq('/cases/executed',3),eq('/assurance/acceptedCandidates',0),eq('/acceptedSuite/version','suite-v2'),eq('/coverage/hiddenMissingObligations',[]),eq('/release/eligible',false)]),
  v('T-91',3,'Requirement revision creates successor evaluations with historical results intact',
    {old:'requirement-v2',next:'requirement-v3',oldSuite:'suite-v2',oldResult:'result-v2',context:'ctx8'},
    ['Accept the reviewed requirement revision and inspect affected test definitions and result applicability before asynchronous regeneration completes.','Generate and review the successor suite with ctx8, reopening the previous suite/result; attempt release using the old passing result on the changed requirement.'],
    [eq('/oldResult/currentApplicability','stale'),eq('/oldResult/originalRequirement','requirement-v2'),eq('/newSuite/context','ctx8'),eq('/history/oldResultOverwritten',false),eq('/oldResult/authorizesNewRelease',false)]),
  v('T-91',4,'Mandatory failure cannot be averaged away by business or technical scores',
    {criteria:{latency:{actualMs:90,maximumMs:100},authority:{requiredSource:'identity-a',actualSource:'replica-b'}},mandatory:'authority',reviewer:'independent-owner'},
    ['Evaluate the candidate with passing latency but failed source authority and present both results with applicability and evidence to the reviewer.','Record a decision to revise the design and rerun the authority evaluation; try publishing an aggregate pass while the mandatory result is still failed.'],
    [eq('/criteria/latency','pass'),eq('/criteria/authority','fail'),eq('/aggregate/releaseEligible',false),eq('/resolution/state','revision-required'),eq('/mandatoryFailure/hiddenByScore',false)]),
  v('T-92',1,'Multi-repository release pins bases checks artifacts and compatibility',
    {repositories:[{id:'service',base:'service-base-a'},{id:'client',base:'client-base-b'}],order:['expand-schema','service','client','contract-schema'],contract:'contact-api-v2',artifacts:['service-image-a','client-bundle-b']},
    ['Prepare the coordinated change from immutable bases, required independent checks and an expand-contract plan; build in isolated workers and record each artifact digest.','Execute the authorized test-environment release order and verify each transition against old/new client and service compatibility before final contract removal.'],
    [eq('/bases/missingOrFloating',[]),eq('/checks/missingRequired',[]),eq('/release/order',['expand-schema','service','client','contract-schema']),eq('/artifacts/unboundToBuild',[]),eq('/compatibility/failedMatrixRows',[])]),
  v('T-92',2,'Drift substituted dependencies and irreversible migration block old release',
    {cases:['service-head-changed','dependency-digest-substituted','drop-column-without-recovery'],approval:'release-a',expectedPlan:'plan-v2'},
    ['Inject each fault after review and before deployment, resolving actual repository heads, dependency bytes and migration effect classification independently.','Attempt release with the old approval; inspect re-evaluation or owned forward-recovery requirements without silently updating the approved bases or recovery plan.'],
    [eq('/cases/executed',3),eq('/release/dispatched',0),eq('/approval/implicitlyRebound',false),eq('/recovery/missingOwnedBlocker',[]),eq('/plan/originalVersion','plan-v2')]),
  v('T-92',3,'Partial repository success resumes per operation without imaginary atomic merge',
    {service:{merge:'merged-a',build:'passed'},client:{merge:'pending',build:'not-started'},faults:['service-merge-response-lost','client-build-fails'],logicalChange:'change-a'},
    ['Accept a service merge remotely, lose its response and restart the coordinator; reconcile its provider key before continuing the client repository.','Fail the client build and resume only eligible remaining work after an explicit correction/review; compare provider history, saved partial status and unchanged completed service operation.'],
    [eq('/service/logicalMerges',1),eq('/service/historyErased',false),eq('/partial/wholeReleaseSucceeded',false),eq('/client/recoveryOwned',true),eq('/coordinator/claimsAtomicCrossProviderCommit',false)]),
  v('T-92',4,'Schema-aware rollback selects compatible recovery and rejects data loss',
    {cases:['additive-column-old-artifact-compatible','old-column-dropped-new-writes-present'],oldArtifact:'service-v1',current:'service-v2',recoveryOwner:'release-owner'},
    ['Assess rollback from both schema states separately using actual compatibility fixtures, migration checkpoints and writes made since deployment.','Allow only the reviewed compatible artifact rollback; refuse destructive automatic database restoration in the second case and preserve the forward-recovery plan and affected writes.'],
    [eq('/compatible/rollbackEligible',true),eq('/destructive/automaticRestore',false),eq('/newWrites/lost',0),eq('/blocked/recoveryOwner','release-owner'),eq('/history/migrationsErased',false)]),
  v('T-93',1,'Architecture alternatives expose constraints assumptions and reversal cost',
    {alternatives:['authority-api','replica-direct'],mandatory:'Party.email-source-authority',costAssumption:'10000-requests-per-day',reviewer:'architecture-owner'},
    ['Compare both alternatives under the same pinned business context, listing authority/data fitness, failure modes, estimated cost inputs and reversal steps with evidence quality.','Reject replica-direct for the mandatory authority violation and record the eligible selection with rationale; inspect that cheaper estimated cost is not permission or measured savings.'],
    [eq('/comparison/missingDimensions',[]),eq('/selection/id','authority-api'),eq('/rejected/reason','mandatory-authority-violation'),eq('/cost/assumption','10000-requests-per-day'),eq('/estimate/claimedMeasured',false)]),
  v('T-93',2,'Regeneration respects human ownership and forces explicit semantic merge',
    {base:'branch-base-a',humanOwned:['src/consent.mjs','model/consent-policy'],humanHead:'human-b',generated:'candidate-c',oldApproval:'approval-a'},
    ['Regenerate code and architecture from base-a while human-b changes both owned paths, then attempt applying candidate-c without resolving the three-way diff.','Inspect preserved human bytes and conflict ownership; resolve explicitly into a new candidate and require fresh checks/approval bound to that resolved digest.'],
    [eq('/conflict/paths',['model/consent-policy','src/consent.mjs']),eq('/human/changesSilentlyOverwritten',false),eq('/merge/unreviewedPublished',false),eq('/oldApproval/currentlyUsable',false),eq('/resolved/newCandidateRequired',true)]),
  v('T-93',3,'Cancelled or failed generation cannot partially publish accepted architecture',
    {cases:['cancel-after-first-generated-file','provider-fails-mid-response'],accepted:'architecture-v3',workingHead:'human-b',staging:'generation-attempt-c'},
    ['Inject each fault in an isolated staging attempt while the accepted architecture and user branch remain available through normal queries.','Restart and inspect terminal attempt state, recoverable restricted partial artifacts and budget reconciliation; verify no incomplete generated file reached the accepted branch or baseline.'],
    [eq('/accepted/version','architecture-v3'),eq('/working/head','human-b'),eq('/partial/publications',0),eq('/attempts/recoverableHistory',true),eq('/uncertainUsage/forgotten',false)]),
  v('T-93',4,'Accepted architecture delta updates canonical refinement with atomic invalidation',
    {system:'contact-service',property:'Party.email',base:'b7',delta:'architecture-delta-v4',affected:['consumer-contract-eval','release-approval'],barrier:'commit-before-view-refresh'},
    ['Review the architecture delta and apply it through the same Studio change command used by authorized form/map edits, preserving stable system/property identity.','Hold projections behind commit and inspect refinement links, authoritative stale consumers, one logical outbox event and independently queued reevaluation before old approval use.'],
    [eq('/system/id','contact-service'),eq('/refinement/delta','architecture-delta-v4'),eq('/invalidation/missingAffected',[]),eq('/outbox/logicalDeltaEvents',1),eq('/staleApproval/dispatches',0)]),
  v('T-94',1,'Promotion needs actual digest configuration and complete health window',
    {candidate:'artifact-a',config:'cfg3',target:'isolated-stage',approval:'independent-a',windowSeconds:300,requiredControls:['health','source-authority']},
    ['Approve the exact target tuple and promote through the broker, then independently inspect running artifact/configuration instead of trusting deployment acceptance.','Collect both controls over the predeclared complete 300-second window and inspect technical acceptance plus retained receipts; a queued deploy or early health sample is insufficient.'],
    [eq('/running/binding',{artifact:'artifact-a',config:'cfg3',target:'isolated-stage'}),eq('/window/coveredSeconds',300),eq('/controls/missingOrFailed',[]),eq('/technical/state','accepted'),eq('/acceptance/basedOnlyOnDispatchAck',false)]),
  v('T-94',2,'Changed target trust evidence or data policy blocks promotion',
    {cases:['stage-to-production-swap','signature-not-currently-trusted','required-check-stale','restricted-data-egress'],candidate:'artifact-a',approval:'stage-approval'},
    ['Mutate each reviewed deployment tuple or current eligibility condition independently just before broker dispatch, retaining the earlier passing build.','Attempt promotion through UI and direct API and inspect current denial, no provider deployment and no exposure of classified configuration or data.'],
    [eq('/cases/executed',4),eq('/provider/deployments',0),eq('/buildPass/overridesCurrentChecks',false),eq('/restricted/disclosures',0),eq('/denials/missingReason',[])]),
  v('T-94',3,'Lost deploy response and canary failure reconcile before compatible recovery',
    {cases:['provider-accepted-response-lost','canary-health-failed'],operation:'deploy-op-a',candidate:'artifact-a',previous:'artifact-old',schema:'backward-compatible'},
    ['Exercise each fault on a clean isolated target, persisting provider identity before restarting the deployment coordinator.','Read actual target state; the timeout case resolves the original operation without duplicate deploy, while failed canary requires current approved compatible rollback and a new observation window.'],
    [eq('/timeout/duplicateDeploys',0),eq('/recovery/targetStateInspected',true),eq('/canary/unapprovedRollback',false),eq('/rollback/artifact','artifact-old'),eq('/failedCanary/erasedFromHistory',false)]),
  v('T-94',4,'Technical acceptance cannot invent missing business outcomes',
    {technical:'accepted',businessMetric:'completed-onboardings',requiredWindow:'2026-10-01/2026-10-08',observations:null},
    ['Complete the qualified technical observation while the business measure source is unavailable for the required window.','Inspect release, process and readiness views after reload; assign the missing measurement without converting an empty query, process diagram or deployment success into a business pass.'],
    [eq('/technical/state','accepted'),eq('/business/state','unknown'),eq('/business/zeroInferred',false),eq('/measurement/ownedGap',true),eq('/readiness/fullBusinessSuccess',false)]),
  v('T-95',1,'Incident links actual bypass to design and reviewed corrective case',
    {trace:'bypass-trace-a',process:'onboarding',property:'Party.email',system:'contact-service',release:'release-a',reviewer:'incident-owner'},
    ['Observe an authority bypass in the isolated production-like fixture and ingest its provenance without changing accepted authority.','Follow the incident to process/property/system/release and open a corrective proposal; record an eligible review decision to proceed with scoped investigation and new evaluations.'],
    [eq('/incident/missingDesignReleaseLinks',[]),eq('/correction/sourceTrace','bypass-trace-a'),eq('/review/decision','investigate-scoped-correction'),eq('/accepted/authorityAutoChanged',false),eq('/correction/automaticallyDeployed',false)]),
  v('T-95',2,'Automatic remediation cannot self-approve or recycle expired proof',
    {agent:'remediation-agent',principal:'requester-a',oldApproval:'approval-expired',proposed:['policy-p4','design-b8']},
    ['Let the scoped agent propose both corrective changes and attempt accepting its own proposal through the alternate module API.','Attempt release using expired evidence and inspect blocked approval/dispatch with preserved proposed artifacts and a route to an independent current reviewer.'],
    [eq('/selfApproval/accepted',false),eq('/expiredProof/dispatches',0),eq('/proposals/preserved',2),eq('/policy/automaticallyActivated',false),eq('/review/independentStillRequired',true)]),
  v('T-95',3,'Recurrence after rollback retains distinct episodes and prior causation',
    {first:'incident-a',rollback:'rollback-a',later:'incident-b',sameSymptom:'replica-read',previousDecision:'decision-a'},
    ['Record the first incident, reviewed rollback and its actual result, then ingest a later distinct event with the same symptom and a duplicate delivery of that later event.','Inspect recurrence links and deduplication: the new episode is not overwritten or dropped as old noise; prior evidence, decision and release history remain navigable.'],
    [eq('/episodes/ids',['incident-a','incident-b']),eq('/duplicate/extraEpisodes',0),eq('/recurrence/priorIncident','incident-a'),eq('/history/priorDecision','decision-a'),eq('/rollback/claimedPreventsAllRecurrence',false)]),
  v('T-95',4,'Corrective learning creates new context with falsifiable observation target',
    {oldBaseline:'b7',newBaseline:'b8',newContext:'ctx8',metric:'unauthorized-source-reads',target:0,cases:[{id:'effective',observed:0},{id:'ineffective',observed:2}],window:'2026-10-01/2026-10-08'},
    ['Independently accept the corrective design and build new context/evaluations under the same common publication and release rules.','Observe each complete-window outcome independently, preserving both corrected and failed results; the ineffective correction reopens owned work rather than rewriting the threshold or prior incident.'],
    [eq('/binding',{baseline:'b8',context:'ctx8'}),eq('/effective/outcome','verified-for-window'),eq('/ineffective/outcome','refuted-for-window'),eq('/ineffective/ownedFollowup',true),eq('/history/b7Rewritten',false)]),
  v('T-96',1,'Authorized service commitment reserves capacity and records one test effect',
    {workflow:'service-fulfilment-v2',commitment:'order-a',units:2,capacity:3,adapter:'isolated-service-adapter',approval:'independent-a',effectKey:'fulfil-a'},
    ['Create a reviewed sandbox service order, reserve two capacity units atomically and dispatch the approved exact commitment through the qualified adapter.','Lose the local response and replay the same command; inspect real isolated provider receipt, one logical effect and reconciled capacity without treating test fulfilment as a live commercial transaction.'],
    [eq('/provider/logicalEffects',1),eq('/commitment/id','order-a'),eq('/capacity/consumedUnits',2),eq('/capacity/availableUnits',1),eq('/evidence/actualProviderReceipt',true)]),
  v('T-96',2,'Generated business actions do not establish regulated or physical authority',
    {cases:['payment-adapter-missing','legal-authority-missing','physical-actuator-unqualified'],process:'generated-enterprise-plan',actor:'enterprise-designer'},
    ['Generate each requested business action as a proposal and attempt enabling or directly dispatching it without its required adapter or authority.','Inspect the owned unsupported/configuration gap and permitted manual planning alternative; verify that neither a high-level approval nor a success checkbox fabricates real execution.'],
    [eq('/cases/executed',3),eq('/realActions/dispatched',0),eq('/generated/implicitCapabilities',0),eq('/physical/fabricatedCompletion',false),eq('/gaps/missingOwnerOrNextAction',[])]),
  v('T-96',3,'Unknown business effect remains reserved until provider reconciliation',
    {effectKey:'fulfil-a',providerOperation:'remote-a',fault:'accepted-before-timeout',reservation:'capacity-a'},
    ['Accept the isolated external action but suppress the response, crash the worker and attempt ordinary retry and compensation while the outcome is unknown.','Inspect unknown-effect status and retained reservation, then reconcile remote-a and record completion from actual evidence without another fulfilment or premature compensation.'],
    [eq('/beforeReconcile/state','unknown-effect'),eq('/beforeReconcile/reservationHeld',true),eq('/provider/logicalFulfilments',1),eq('/prematureCompensation/effects',0),eq('/afterReconcile/state','completed')]),
  v('T-96',4,'Partial business fulfilment uses separately authorized compensation',
    {steps:['reserve-service','issue-test-ticket','notify-recipient'],succeeded:['reserve-service','issue-test-ticket'],failed:'notify-recipient',irreversible:'issue-test-ticket',compensation:'cancel-test-ticket'},
    ['Fail notification after the first two isolated effects and inspect the durable per-effect state and commitment before requesting recovery.','Reject unapproved compensation; authorize the supported cancellation and independently observe its result, preserving ticket issuance and cancellation as distinct history rather than one atomic rollback.'],
    [eq('/partial/succeededSteps',['reserve-service','issue-test-ticket']),eq('/unapprovedCompensation/effects',0),eq('/compensation/separateEvidence',true),eq('/history/issuanceErased',false),eq('/workflow/claimsAtomicRollback',false)]),
  v('T-97',1,'Connector onboarding qualifies every required class with scoped manifests',
    {requiredClasses:['artifact','identity','model','scm','secret','deployment','business-service'],manifest:'adapter-support-v1',scope:'tenant-a/workspace-a',credentialLocation:'server-secret-reference'},
    ['Select one explicitly supported adapter per required class from the pinned release manifest, run authorized isolated connectivity and conformance checks, and inventory all external dependencies.','Inspect immutable adapter versions, reads/effects/events/retention/reconciliation and least-privilege scope against actual provider grants; activating a connection does not activate business effect permission.'],
    [eq('/classes/missingQualifiedAdapter',[]),eq('/manifests/missingRequiredFields',[]),eq('/credentials/browserSecretValues',0),eq('/scopes/broaderThanConfigured',[]),eq('/connection/implicitBusinessAuthority',false)]),
  v('T-97',2,'Webhook replay altered schema and excessive scopes do not mutate accepted state',
    {cases:['same-signed-event-replayed','same-id-changed-payload','invalid-signature','overbroad-grant','breaking-schema-change'],event:'evt-a',source:'crm-a'},
    ['Deliver each fault independently after a valid control event, validating raw bytes, tenant/source binding, signing policy, event identity and schema before durable admission.','Inspect safe replay acknowledgment for identical delivery or rejected/quarantined diagnostics for invalid input; compare accepted records and downstream effect counts, including archived raw evidence access restrictions.'],
    [eq('/cases/executed',5),eq('/replay/logicalEvents',1),eq('/invalid/acceptedMutations',0),eq('/invalid/dispatchedEffects',0),eq('/diagnostics/missingDisposition',[])]),
  v('T-97',3,'Connector retry rotation and cursor recovery preserve acknowledged work',
    {sourceEvents:['e1','e2','e3'],cursor:'c0',faults:['rate-limit-before-page','crash-after-inbox-commit-before-ack','credential-rotation-mid-backfill'],retryProfile:{maxAttempts:3,maxElapsedSeconds:60}},
    ['Execute each fault separately with a scripted isolated source, persisting inbox records and checkpoint atomically before acknowledging delivery or advancing a cursor.','Restart with scoped replacement credentials, honor retry bounds/provider delay and reconcile the full event set; exhaustion creates owned paused work, never a silently skipped page or fresh full budget.'],
    [eq('/acknowledged/lostEventIds',[]),eq('/events/logicalIds',['e1','e2','e3']),eq('/effects/duplicated',0),lte('/retry/attemptsBeforeEscalation',3),eq('/revokedCredential/usable',false)]),
  v('T-97',4,'Connector disable drains or reconciles work and revokes cached access',
    {connector:'crm-a',queued:'job-b',remoteAccepted:'remote-a',cache:'retrieval-a',download:'download-a',mode:'disable-and-reconcile'},
    ['Preview all active consumers and request disable under a reviewed drain/reconciliation policy with a separately scoped recovery capability.','Block new normal work and cached disclosure immediately under the current security epoch; reconcile the already-accepted effect, then finish disable only after pending ownership is resolved.'],
    [eq('/afterDisable/newNormalDispatches',0),eq('/cached/authorizedDisclosures',0),eq('/remote/silentlyAbandoned',false),eq('/impact/missingActiveConsumer',[]),eq('/recovery/broadGrantRetained',false)]),
  v('T-98',1,'Role homes preserve scoped navigation without granting persona authority',
    {roles:['founder','worker','architect','administrator'],jobs:['design-business','perform-assigned-task','review-system-impact','configure-installation'],context:{workspace:'workspace-a',baseline:'b7',selection:'process-a'},hiddenMarker:'FOREIGN_OBJECT_CANARY'},
    ['Open each authenticated role home and perform its named primary navigation, using permission-filtered search and command palette across the linked module screens.','Return through graph/list/detail and browser navigation, compare retained scoped context and inspect direct URL/search suggestions for hidden objects; changing persona must not grant permissions.'],
    [eq('/roles/completedPrimaryNavigation',4),eq('/navigation/contextLosses',0),eq('/persona/grantsAuthority',false),eq('/hidden/searchOrSuggestionHits',0),eq('/navigation/semanticMutations',0)]),
  v('T-98',2,'Accessible editors and interventions work without canvas or pointer',
    {modes:['keyboard-only','screen-reader','400-percent-zoom'],actions:['edit-workflow-criterion','review-bound-release','pause-agent'],viewport:{width:1280,height:800}},
    ['Run every mode/action pair on the pinned supported browser/assistive-technology profile, using structured alternatives for diagram edits and inspecting the actual saved command result.','Record focus order, names/state announcements, error focus, dialog return and sticky-control visibility; verify no mandatory action requires dragging, color perception or inaccessible offscreen controls.'],
    [eq('/matrix/completedPairs',9),eq('/actions/missingPersistedResult',[]),eq('/focus/traps',0),eq('/commands/obscuredOrUnreachable',0),eq('/pointer/mandatoryActions',0)]),
  v('T-98',3,'Slow sessions and conflicts retain only permitted drafts with safe recovery',
    {cases:['slow-old-response-arrives-last','session-expires','concurrent-head-change'],draft:'draft-a',scope:'workspace-a',revocationCase:'membership-revoked-before-reauth'},
    ['Exercise the three failures separately during an authorized draft edit; inspect loading/stale/session/conflict states, command status readback and explicit three-way rebase.','Reauthenticate as the same still-authorized principal to recover the permitted draft; separately revoke membership and verify sensitive draft/cache content is no longer disclosed or replayed into another tenant.'],
    [eq('/permitted/draftLost',false),eq('/oldResponse/overwritesNewContext',false),eq('/recovery/blindMutationReplay',false),eq('/revoked/contentDisclosed',false),eq('/conflict/explicitResolutionRequired',true)]),
  v('T-98',4,'Independent laptop study records completion assistance and readiness errors',
    {groups:['founder-process-owner','frontline-worker','enterprise-data-architect','engineer-release','policy-review-audit','customer-admin'],minimumPerGroup:2,maximumGroupsPerPerson:2,minimumCompletionPercent:90,profile:'PORTFOLIO-UX-role-study'},
    ['Recruit the required independent representatives and accessibility evaluators, freeze task/environment matrix and denominators, and run the specified non-bank, finance, imported and multi-entity journeys without teaching internals.','Record attempts, completion, assistance, timing, backtracking and design-versus-readiness misunderstandings; independently assess critical findings and rerun remediation instead of fabricating participant results from browser scripts.'],
    [eq('/sampling/missingGroups',[]),lte('/tasks/noncompletionPercent',10),eq('/effects/unauthorizedOrUnintended',0),eq('/findings/unresolvedCritical',0),eq('/observations/missingAssistanceOrReadinessMeasures',[])]),
  v('T-99',1,'Customer installation uses declared local services and customer identities',
    {host:'clean-supported-linux',modules:['studio','sdlc','sentinel','warden','arbiter','ledger','overseer','steward'],release:'release-lock-a',services:['postgres','artifact-store','workflow-runtime'],operator:'customer-admin'},
    ['Have an independent operator install the signed pinned module/dependency closure on a clean host using the runbook and customer-owned identity/secrets, with approved test provider configuration.','Boot, authenticate and restart a small design plus durable workflow, then inspect actual storage/network dependencies and persisted state; no developer credentials or undisclosed hosted data service may be required.'],
    [eq('/install/developerCredentialsRequired',false),eq('/dependencies/undeclaredHostedServices',[]),eq('/restart/lostAcknowledgedState',0),eq('/runtime/pinnedAndHealthy',true),eq('/operator/developerAssistanceRequired',false)]),
  v('T-99',2,'Unavailable dependencies block affected commands not unrelated authorized reads',
    {cases:['required-warden-missing','workflow-runtime-version-incompatible','optional-model-provider-offline'],read:'authorized-saved-blueprint',effect:'new-protected-agent-action'},
    ['Exercise each dependency fault independently and inspect installation health, affected command diagnostics and the unchanged selected module manifest.','Attempt protected execution and an unrelated permitted saved-blueprint read; verify no permissive policy or second process scheduler is started as an availability fallback.'],
    [eq('/affected/protectedDispatches',0),eq('/unrelated/authorizedReadsSucceeded',3),eq('/dependencies/missingActionableDiagnostic',[]),eq('/fallback/permissiveModeEnabled',false),eq('/fallback/secondSchedulerStarted',false)]),
  v('T-99',3,'Interrupted upgrade or disable preserves one compatible owner and queued history',
    {cases:['upgrade-after-expand-before-cutover','disable-with-active-runtime-history'],oldRelease:'release-a',newRelease:'release-b',queue:['run-a','effect-b'],revoked:'grant-old'},
    ['Inject each failure separately after a verified consistent backup and supported drain/migration plan, preserving domain, artifact and workflow-runtime recovery checkpoints.','Recover to the recorded compatible release set through tested rollback or forward recovery, fence obsolete workers and reconcile provider effects before resuming queued work.'],
    [eq('/recovery/mixedUnsupportedVersions',[]),eq('/queue/lostAcknowledgedIds',[]),eq('/effects/duplicateLogicalDispatches',0),eq('/oldOwner/newDispatches',0),eq('/restore/revokedGrantRevived',false)]),
  v('T-99',4,'Disconnected profile verifies mirrored dependencies and discloses disabled cloud work',
    {profile:'disconnected-local-design-and-workflow',network:'external-egress-blocked',mirror:'signed-mirror-a',trustAnchor:'customer-pinned-root',cloudOnly:['hosted-model','external-deploy'],localProvider:'qualified-local-model'},
    ['Install and verify the complete signed dependency closure from the local mirror against an independently pinned trust anchor, including runtime services and a separately configured local provider.','Run the declared local subset while capturing egress; inspect unavailable cloud features and repeat with tampered/missing mirror entries, rejecting incomplete or untrusted installation rather than fetching hidden fallbacks.'],
    [eq('/subset/missingRequiredEvidence',[]),eq('/network/externalCalls',0),eq('/cloudOnly/labeledEnabled',[]),eq('/tamperedOrMissing/installAccepted',false),eq('/trust/anchorTakenOnlyFromBundle',false)])
];
