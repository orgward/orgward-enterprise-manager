import {ruleProfile,ruleFixtures} from './sentinel-rule-fixtures-12.mjs';
const eq=(path,value)=>({path,op:'eq',value});
const lte=(path,value)=>({path,op:'lte',value});
const v=(task,ac,name,initial,steps,assertions)=>({id:`DV-${task}-AC${ac}`,taskId:task,acceptanceId:`${task}-AC${ac}`,name,initial,steps,assertions,status:'not_run',reviewStatus:'pending',reviewer:null,testPath:`tests/acceptance/${task.toLowerCase()}.test.mjs`});
const ruleAssertions=rows=>rows.flatMap(r=>[eq(`/rules/${r.ruleId}/triggerOutcome`,r.triggerOutcome),eq(`/rules/${r.ruleId}/canonicalSeverity`,r.severity),eq(`/rules/${r.ruleId}/controlOutcome`,r.controlOutcome)]);
export const domainVectors=[
  v('T-69',1,'Typed claim mapping retains classification and every provenance edge',
    {claims:['ownership-capability','ownership-entity','authority','realization','integration','decision','policy','classification'],entity:'Party',system:'identity',scope:'LE:A',duplicateAuthorityEvidence:['adr-a','adr-b']},
    ['Compile accepted typed fixtures for every listed mapping plus duplicate authority provenance, using the pinned graph and rule profile.','Inspect canonical graph bytes and evidence links after rerun; verify classification is scoped node semantics, not an invented relationship or a label-derived identity.'],
    [eq('/mapping/missingExpectedPredicates',[]),eq('/classification/value','canonical_identity'),eq('/authority/distinctSemanticEdges',1),eq('/authority/evidenceLinks',2),eq('/rerun/semanticByteDifferences',0)]),
  v('T-69',2,'Transfers preserve entity-specific semantic relationship identities',
    {from:'system-a',to:'system-b',scope:'LE:A',transferredEntities:['Party','Account'],duplicatePartyClaim:'claim-party-copy'},
    ['Compile the two transfers with identical endpoints and scope but different entity properties, including a second evidence-backed Party claim.','Recompile with shuffled claim order and inspect edge identity, entity properties and retained provenance independently of canvas edge rendering.'],
    [eq('/transfers/semanticCount',2),eq('/transfers/entities',['Account','Party']),eq('/party/provenanceClaimCount',2),eq('/rerun/changedEdgeIds',[]),eq('/graph/parallelEdgeHiddenAsDuplicate',false)]),
  v('T-69',3,'OADL round trip preserves supported typed references and discloses loss',
    {profile:'oadl-v0.1',objects:['domain:retail','capability:onboard','entity:Party','system:identity'],extra:['workflow-loop','claim-review-history','temporal-scope'],target:'isolated-import'},
    ['Export the supported subset with the ten allowed top-level collections and a separate loss/provenance manifest for richer graph content.','Validate and reimport the projection with its identity crosswalk; compare supported typed references, inspect each omission and attempt treating the export as a complete workflow backup.'],
    [eq('/projection/unknownTopLevelCollections',[]),eq('/roundTrip/changedSupportedIds',[]),eq('/roundTrip/danglingTypedReferences',[]),eq('/loss/reported',['claim-review-history','temporal-scope','workflow-loop']),eq('/projection/claimedFullBackup',false)]),
  v('T-69',4,'Unknown scope remains uncertainty rather than global or disjoint',
    {claims:[{authority:'system-a',scope:'unknown'},{authority:'system-b',scope:'LE:A'}],entity:'Party',profile:ruleProfile},
    ['Compile the potentially conflicting authority claims while preserving unknown scope as an explicit semantic value.','Inspect R-25 and dependent mandatory-authority queries; compare to separate explicit-global and proven-disjoint controls without filling missing scope from defaults.'],
    [eq('/scope/inferredGlobal',false),eq('/scope/assumedDisjoint',false),eq('/findings/rule','R-25'),eq('/authority/overlap','unknown'),eq('/protected/allowedFromUncertainty',false)]),
  v('T-70',1,'Every base rule has a concrete trigger and nontrigger observation',
    {profile:ruleProfile,cases:ruleFixtures.slice(0,15)},
    ['Expand every R-01 through R-15 trigger and control into separate complete typed seeds, isolating the target predicate and declaring unavoidable companion findings.','Run compiler or declared staging diagnostics and collect target rule outcome, canonical severity, exact involved IDs, reason and evidence; compare ordered and shuffled reruns.'],
    [...ruleAssertions(ruleFixtures.slice(0,15)),eq('/matrix/executedCases',30),eq('/findings/missingExplanations',[]),eq('/rerun/changedSemanticResults',[])]),
  v('T-70',2,'Single-source policy locks a contradiction regardless of confidence',
    {entity:'Party',classification:'canonical_identity',authorities:['system-a','system-b'],confidence:[0.99,0.1],policy:'single_authority',userSeverityPreference:'low'},
    ['Compile both accepted authorities under overlapping scope and the single-source policy, then attempt lowering severity in the user preference UI.','Inspect one grouped contradiction case with independently retained R-01 and R-09 reasons, evidence and protected-use decision.'],
    [eq('/contradiction/groupedCases',1),eq('/contradiction/ruleReasons',['R-01','R-09']),eq('/contradiction/canonicalSeverity','CRITICAL'),eq('/authority/lowConfidenceDiscarded',false),eq('/preference/weakenedEnforcement',false)]),
  v('T-70',3,'Incomplete coverage and manual work do not fabricate contradictions',
    {cases:['source-coverage-incomplete','authority-system-kind-unknown','owned-capability-intentionally-manual'],manualEvidence:'approved-human-realization'},
    ['Evaluate each case independently under an explicit applicability profile, retaining the source coverage gap, unknown kind or manual realization.','Inspect scoped findings and repair actions; ensure an unknown has not become proof of zero authority and a valid manual process is not forced into software generation.'],
    [eq('/coverage/status','unknown'),eq('/kind/rule','R-11'),eq('/manual/r14Applicable',false),eq('/fabricatedCriticalContradictions',0),eq('/manual/codeGenerationRequired',false)]),
  v('T-70',4,'Reviewed correction resolves only affected findings after publication',
    {findings:['authority-conflict-party','ownership-conflict-account'],repair:'supersede-party-authority-b',decision:'review-12',oldSnapshot:'snapshot-7'},
    ['Apply the eligible authority supersession with exact review binding while pausing compile publication, leaving the account ownership conflict unchanged.','Inspect both findings before publication and after the matching new snapshot; open the historical Party explanation and its repair decision.'],
    [eq('/beforePublish/resolvedCount',0),eq('/afterPublish/resolvedIds',['authority-conflict-party']),eq('/afterPublish/openIds',['ownership-conflict-account']),eq('/history/originalExplanationRetained',true),eq('/resolution/review','review-12')]),
  v('T-71',1,'Execution-derived replica reads expose property authority bypass',
    {extension:'property-process-v1',property:'Party.email',authority:'identity',readSource:'crm-replica',process:'onboarding',step:'verify-contact',run:'run-1',evidence:'read-trace-1'},
    ['Ingest an authorized execution-read observation under the explicit extension profile and compile it against the accepted property authority.','Open R-26 from the process and SDLC context views, following the exact property, step, source and evidence; inspect proposed remediation without changing accepted authority.'],
    [eq('/finding/rule','R-26'),eq('/finding/severity','HIGH'),eq('/trace/missingRequiredLinks',[]),eq('/accepted/authority','identity'),eq('/remediation/automaticallyExecuted',false)]),
  v('T-71',2,'Explicit inheritance differs from missing authority and source divergence',
    {property:'Party.email',entityAuthority:'identity',run:'run-1',stepSources:['identity','replica'],cases:['accepted-inheritance','no-inheritance-declaration'],scope:'LE:A'},
    ['Compile both case seeds with otherwise identical observed steps; retain an explicit accepted inheritance decision only in the first.','Inspect R-27 applicability and R-28 conflicting read-source evidence per run; do not infer property inheritance from absence of an override record.'],
    [eq('/inherited/r27Emitted',false),eq('/missing/r27Emitted',true),eq('/inherited/r28Emitted',true),eq('/missing/authorityInferred',false),eq('/divergence/processRun','run-1')]),
  v('T-71',3,'Stale observations and unsupported profiles cannot resolve compliance',
    {cases:['observation-expired','source-removed-without-retraction','required-extension-v99'],lastGood:'lineage-snapshot-7',finding:'r26-open'},
    ['Evaluate each fault independently and inspect coverage or compatibility diagnostics while retaining the last-good snapshot and previous finding history.','Attempt using the incomplete result to resolve the violation or authorize dependent release; inspect stale labeling and owned source/profile recovery.'],
    [eq('/uncertainty/convertedToResolution',false),eq('/unsupported/profilePublished',false),eq('/history/lastGoodPreserved',true),eq('/release/authorized',false),eq('/ui/recoveryMissing',false)]),
  v('T-71',4,'Every advanced rule retains source meaning and profile-specific outcome',
    {profile:ruleProfile,cases:ruleFixtures.slice(15)},
    ['Materialize separate trigger/control seeds for R-16 through R-28, including legacy exception diagnostics, strict threshold boundaries and duplicate typed IDs.','Run the matching rule or prepublication validator; verify heuristic labeling, failure without partial publication for R-24 and stable target outcomes under input reordering.'],
    [...ruleAssertions(ruleFixtures.slice(15)),eq('/matrix/executedCases',26),eq('/r16/labeledHeuristic',true),eq('/r24/partialSnapshotPublished',false),eq('/rerun/changedSemanticResults',[])]),
  v('T-72',1,'First-session inbox prioritizes actionable findings and exposes evidence',
    {findings:[{id:'f-critical',severity:'CRITICAL',category:'contradiction'},{id:'f-high',severity:'HIGH',category:'policy'},{id:'f-unknown',severity:'WARN',category:'unknown'},{id:'f-drift',severity:'INFO',category:'drift'}],sameScopeAndTimestamp:true,actor:'new-architect'},
    ['Enter Sentinel from authorized sample-sized sources without a completed enterprise model and inspect the default priority order with category filters.','Use keyboard queue navigation to open the first three findings and their exact evidence, then return to the retained selection and inspect permitted repair choices.'],
    [eq('/inbox/defaultOrder',['f-critical','f-high','f-unknown','f-drift']),eq('/navigation/fullModelRequired',false),eq('/evidence/missingPointers',[]),eq('/keyboard/inaccessibleActions',[]),eq('/return/selectionPreserved',true)]),
  v('T-72',2,'Acknowledgment cannot dismiss mandatory release blockers',
    {finding:'f-critical',state:'open',release:'candidate-a',actions:['acknowledge','dismiss-notification','snooze-notification']},
    ['Perform each notification action independently as a permitted reader while the underlying contradiction and policy remain unchanged.','Reload the finding and attempt the protected release through the API; inspect notification metadata separately from semantic resolution and enforcement.'],
    [eq('/finding/state','open'),eq('/release/effects',0),eq('/notification/treatedAsResolution',false),eq('/history/actionsRecorded',true),eq('/ui/blockerStillVisible',true)]),
  v('T-72',3,'Failed repair compile retains last-good evidence and safe retry',
    {repair:'supersede-claim-b',lastGood:'snapshot-7',fault:'after-staging-before-publication',selection:'f-critical'},
    ['Submit the reviewed repair and fail compilation at the named boundary before the new snapshot can publish.','Reload the inbox, inspect stale/failed status and retained evidence, then retry the same manifest after recovery; count semantic publications and preserve selection.'],
    [eq('/failure/visibleSnapshot','snapshot-7'),eq('/failure/staleBanner',true),eq('/failure/claimsRepairSucceeded',false),eq('/retry/distinctPublications',1),eq('/ui/selection','f-critical')]),
  v('T-72',4,'Scope-split repair traces resolution to the exact review and delta',
    {finding:'ownership-overlap',oldScope:'global',newScopes:['LE:A','LE:B'],review:'scope-split-review',command:'split-12'},
    ['Preview and independently approve the justified ownership split with non-overlap evidence; supersede the prior unscoped/global claims explicitly.','Publish the matching compile and navigate its added/removed relation delta back to the review and affected finding without rewriting the prior snapshot.'],
    [eq('/resolution/review','scope-split-review'),eq('/current/scopes',['LE:A','LE:B']),eq('/delta/removedGlobalRelations',2),eq('/delta/addedScopedRelations',2),eq('/history/oldSnapshotRetained',true)]),
  v('T-73',1,'Timeboxed exception changes disposition but not canonical severity',
    {finding:'f-critical',severity:'CRITICAL',scope:'LE:A/staging/candidate-a',decision:'independent-review-a',reviewAt:'2026-10-02T00:00:00Z',expiresAt:'2026-10-03T00:00:00Z',compensatingControl:'read-only-shadow',policyAllowsException:true},
    ['Request and independently approve the narrow exception with rationale, evidence and compensating control under a policy that permits this deviation.','Inspect the visible critical finding, separate excepted disposition and dates; attempt applying the exception to production or another artifact without a new decision.'],
    [eq('/finding/severity','CRITICAL'),eq('/finding/disposition','excepted'),eq('/exception/reviewAt','2026-10-02T00:00:00Z'),eq('/exception/expiresAt','2026-10-03T00:00:00Z'),eq('/otherScope/allowed',false)]),
  v('T-73',2,'Invalid or non-overridable exceptions never create permission',
    {cases:['missing-rationale','missing-evidence','expired-reviewer-authority','immutable-deny'],requested:'release-deploy',approval:'present-but-insufficient'},
    ['Attempt exception activation independently for every invalid case, including a correctly signed approval for the immutable-deny case.','Read exception records and capability/effect logs after reload; inspect precise safe repair or prohibition messages without reducing the finding severity.'],
    [eq('/invalid/activeExceptions',0),eq('/capabilities/newGrants',0),eq('/deployment/dispatches',0),eq('/finding/severityReduced',false),eq('/errors/missingReason',false)]),
  v('T-73',3,'Expiry or reviewer revocation blocks queued work at effect time',
    {cases:['expires-exactly-at-dispatch','reviewer-revoked-after-queue'],expiresAt:'2026-10-03T00:00:00Z',owner:'migration-owner',barrier:'after-queue-before-broker-check'},
    ['Queue work with a presently eligible exception, then inject expiry at the exact boundary or revoke the required reviewer in separate runs.','Resume dispatch and inspect current eligibility denial, retained queued-work evidence and an idempotent reassessment item assigned to the accountable owner.'],
    [eq('/afterInvalidation/effects',0),eq('/expiry/equalityTreatedAsExpired',true),eq('/reassessment/owner','migration-owner'),eq('/reassessment/duplicateItems',0),eq('/history/exceptionPreserved',true)]),
  v('T-73',4,'Corrected design resolves a finding without deleting exception history',
    {finding:'f-critical',exception:'ex-a',review:'repair-review-a',oldSnapshot:'snapshot-7',newAcceptedAuthority:'system-a'},
    ['Supersede the conflicting authority through an eligible review and inspect the still-visible exception while compilation is pending.','Publish the corrected snapshot, inspect semantic resolution and ended exception applicability, then independently export the original exception and decision chain.'],
    [eq('/beforeCompile/resolved',false),eq('/afterCompile/findingState','resolved'),eq('/afterCompile/exceptionStillNeeded',false),eq('/audit/exceptionDeleted',false),eq('/resolution/review','repair-review-a')]),
  v('T-74',1,'Adjacent deltas separate relation removal from lost source coverage',
    {snapshots:['s1','s2','s3'],relation:'system-a-realizes-cap-b',changes:['accept-relation','reviewed-supersession-removes-relation']},
    ['Compile s1 then accept one relation to produce s2; explicitly supersede its claim with reviewed correction before compiling s3.','Compare each adjacent snapshot pair and the s1-to-s3 net view, retaining the intermediate evidence instead of deleting a transient relationship from history.'],
    [eq('/delta12/added',1),eq('/delta12/removed',0),eq('/delta23/added',0),eq('/delta23/removed',1),eq('/history/intermediateEvidenceRetained',true)]),
  v('T-74',2,'Connector loss produces coverage drift rather than design retraction',
    {cases:['connector-outage','source-permission-revoked'],acceptedRelation:'identity-authoritative-Party',lastSourceRevision:'source-r7'},
    ['Inject each source failure independently while its accepted authority claim remains unchanged and recompute the coverage assessment.','Compare semantic and coverage deltas in UI and export; attempt to infer removal or a new authority from the disappearance of observations.'],
    [eq('/semantic/removedAuthorityRelations',0),eq('/coverage/state','lost'),eq('/accepted/retractedClaims',0),eq('/authority/inventedReplacement',false),eq('/ui/sourceRecoveryVisible',true)]),
  v('T-74',3,'Late observations preserve both clocks and cannot rewind current state',
    {currentBaseline:'b9',newerObservation:{validAt:'2026-10-02T10:00:00Z',recordedAt:'2026-10-02T10:01:00Z'},lateObservation:{validAt:'2026-10-01T10:00:00Z',recordedAt:'2026-10-03T10:00:00Z'},fault:'older-compile-finishes-last'},
    ['Record the newer observation then the late historical one, with immutable source identity and separate valid/recorded timestamps.','Finish the older compile after the latest one and query current plus bitemporal historical views; inspect the stale-result publication fence and correction lineage.'],
    [eq('/current/baseline','b9'),eq('/late/validAt','2026-10-01T10:00:00Z'),eq('/late/recordedAt','2026-10-03T10:00:00Z'),eq('/staleCompile/promotedHead',false),eq('/history/lateEvidenceDiscarded',false)]),
  v('T-74',4,'Critical observed drift invalidates delivery context without rewriting intent',
    {change:'sdlc-change-a',pinnedSnapshot:'s7',drift:'unexpected-replica-read',evaluation:'eval-a',approval:'approval-a'},
    ['Publish the fresh integrity assessment containing critical observed drift that affects the active change case and its mandatory evidence dependencies.','Inspect stale context/evaluation/approval, attempt the queued protected release and open a linked corrective proposal; ensure observed behavior has not overwritten accepted authority.'],
    [eq('/context/stale',true),eq('/evaluation/stale',true),eq('/oldApproval/effects',0),eq('/correction/status','proposed'),eq('/acceptedAuthority/changedByObservation',false)]),
  v('T-75',1,'Sentinel qualification inventories every source acceptance and rule pair',
    {sourceAcceptanceIds:Array.from({length:33},(_,i)=>`AT-${String(i+1).padStart(2,'0')}`),ruleIds:ruleFixtures.map(r=>r.ruleId),requiredArtifacts:['raw-results','fixture-manifest','revision','profile','environment','source-hashes','independent-review']},
    ['Build the qualification run matrix from the pinned catalogue and local compatibility decisions, expanding each AT plus all rule triggers/controls into actual executable tests.','Run on the supported revision including first-session journeys, deterministic reruns and measured compile loads; independently reconcile all IDs to receipts and inspect unsupported combinations.'],
    [eq('/coverage/missingAcceptanceIds',[]),eq('/coverage/missingRuleIds',[]),eq('/receipts/missingRequiredArtifacts',[]),eq('/coverage/unrunMandatoryCases',0),lte('/performance/fullCompile100kMaxSeconds',120),eq('/performance/warmRuns',10),eq('/performance/coldRuns',3)]),
  v('T-75',2,'Qualification rejects missing or nondeterministic evidence despite UI smoke',
    {cases:['critical-rule-fails','semantic-rerun-differs','AT-24-result-missing'],uiSmoke:'pass'},
    ['Run qualification with each deliberately defective result set separately while retaining passing UI smoke receipts for the same revision.','Inspect blocked qualification and the exact missing or failing requirement; attempt issuing a supported Sentinel release label from the smoke result alone.'],
    [eq('/qualification/passedInvalidCases',0),eq('/diagnostics/missingRequirementId',false),eq('/uiSmoke/substitutedForAcceptance',false),eq('/release/unsupportedReadyLabel',false),eq('/evidence/failedResultsRetained',true)]),
  v('T-75',3,'Compile failures preserve manifests and auditable last-good history',
    {faults:['duplicate-canonical-id','crash-after-staging','publication-transaction-abort'],lastGood:'snapshot-7',inputManifest:'manifest-8',sourceAT:'AT-33'},
    ['Inject every fault independently, inspect failed job diagnostics, last-good snapshot and immutable exact input manifest in the audit export.','Repair the invalid input by a new reviewed manifest or retry the unchanged valid manifest as appropriate; verify one eligible publication and independently reconstruct its provenance.'],
    [eq('/failure/currentSnapshot','snapshot-7'),eq('/failure/partialPublication',false),eq('/export/missingInputVersionReferences',[]),eq('/retry/duplicatePublication',false),eq('/repair/overwrittenFailedManifest',false)]),
  v('T-75',4,'Actionability reports disagreements and false positives without selection bias',
    {strata:{contradiction:8,policy:8,unknown:7,drift:7},reviewers:['domain-a','domain-b'],ratings:{bothActionable:21,aOnly:3,bOnly:2,neither:4},falsePositiveRatings:{a:2,b:3},firstSessionTargetSeconds:900},
    ['Freeze the stratified sample before independent review, retain each raw rating and rationale, and record false-positive labels separately from actionability.','Publish conservative joint and per-reviewer rates with all disagreements and the sampling manifest; execute the first-session flow and retain failure evidence instead of replacing hard findings.'],
    [eq('/sample/size',30),eq('/actionability/joint',{numerator:21,denominator:30,percent:70}),eq('/actionability/reviewerA',{numerator:24,denominator:30,percent:80}),eq('/actionability/reviewerB/numerator',23),eq('/disagreements/count',5),eq('/falsePositives/rawRatingsRetained',true),eq('/sample/postRatingSubstitutions',0)]),
  v('T-76',1,'Bound effect evaluation is deterministic and separates unmet obligations',
    {policy:'policy-v3',interpreter:'typed-policy-v1',tuple:{actor:'deployer-a',scope:'LE:A',target:'staging',artifact:'digest-a',config:'config-v2',context:'context-v7',budget:100,command:'effect-1'},cases:['eligible-with-evidence','eligible-but-review-missing','explicit-target-deny'],clock:'2026-10-01T10:00:00Z'},
    ['Evaluate each complete bound tuple under the same pinned strategic/domain/runtime policy lineage and bound clock, recording matched conditions and required obligations.','Repeat with reordered policy storage and restart; compare semantic decision outputs excluding receipt timestamps and inspect that an obligation result is not an executable grant.'],
    [eq('/decisions/outcomes',['allow','obligations','deny']),eq('/decisions/policy','policy-v3'),eq('/lineage/missingGoverningVersions',[]),eq('/rerun/semanticDifferences',[]),eq('/unmetObligations/grantIssued',false)]),
  v('T-76',2,'Unknown authority and expression injection fail closed before execution',
    {cases:['missing-authority','missing-context-manifest','mandatory-allow-deny-conflict','JavaScript-expression','SQL-expression','network-call-expression'],target:'production'},
    ['Evaluate each malformed or incomplete case independently through the policy API and structured criteria editor with no permissive fallback.','Inspect safe diagnostics and escalation ownership; examine interpreter and broker logs for evaluated arbitrary code, outbound requests or issued capabilities.'],
    [eq('/protected/allowedCases',0),eq('/interpreter/arbitraryCodeExecutions',0),eq('/interpreter/networkCalls',0),eq('/capabilities/issued',0),eq('/errors/missingActionableReason',false)]),
  v('T-76',3,'Policy rollback creates a new activation without reviving revoked grants',
    {active:'policy-v3',rollbackContent:'policy-v2',revokedGrant:'grant-a',securityEpoch:8,oldDecision:'decision-under-v2'},
    ['Review and activate the previous policy content as a new activation revision while keeping current identity, revocation and placement epochs.','Replay the old decision and attempt effect dispatch with the revoked grant; inspect immutable decision history and the current evaluation rather than restoring old capability rows.'],
    [eq('/activation/newRevisionCreated',true),eq('/security/epoch',8),eq('/revokedGrant/usable',false),eq('/oldDecision/treatedAsCurrent',false),eq('/history/oldDecisionPreserved',true)]),
  v('T-76',4,'Arbiter approval cannot override immutable Warden restrictions',
    {request:'deploy-a',exception:'approved-ex-a',arbiter:'eligible-independent-quorum',immutableDeny:'artifact-digest-mismatch',requestedDigest:'digest-a',actualDigest:'digest-b'},
    ['Supply a valid independently approved exception and complete effect tuple, then substitute the actual artifact digest before the protected check.','Inspect immutable-deny precedence and the explained decision in both policy and reviewer views; attempt renaming or removing a workflow review node and repeat the check.'],
    [eq('/decision/outcome','deny'),eq('/decision/reason','artifact-digest-mismatch'),eq('/approval/overrodeImmutableDeny',false),eq('/effects/dispatched',0),eq('/workflowEdit/weakenedBrokerCheck',false)])
];
