# Implementation prompt

Implement the selected `TASKS.md` item in real product code. Add focused behavior
tests, durable state/API behavior and usable UI where applicable. Run the focused
suite and `npm run check`, then hand the diff to implementation review. Do not wait
for paper approval or substitute metadata checks for behavior.
