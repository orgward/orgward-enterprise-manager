# Configurable workflow decision and next foundation scenarios — increment 10

Historical checkpoint. Increment 11 now covers the next T-56–T-67 slice; see
[current handoff](DOMAIN-FIXTURE-HANDOFF-11.md) for cumulative counts. The hard
workflow decision and all obligations below remain mandatory and unimplemented.

The user explicitly made visual/structured SDLC reconfiguration a hard requirement.
Read [CONFIGURABLE-WORKFLOWS](../product/CONFIGURABLE-WORKFLOWS.md) and
[ADR-010](ADR-010-CONFIGURABLE-WORKFLOW.md) first. Sixteen additional WF obligations
are registered in both canonical backlog and architecture. Existing original task
objects, 480 acceptance sentences and dependencies are unchanged. P/E denominators
remain fixed. The workflow obligations are **additional**, not hidden in 480.

## Requirement coverage and enforcement

The contract covers editing parity, typed nodes/actions/criteria, bounded loops,
parallel joins, subprocesses, shared limits, immutable versions/template overrides,
explicit run migration, durable runtime reconciliation, timers/human decisions,
compensation, effect-free simulation, loss-aware interchange, operations and real
end-to-end qualification. The stage rail is a starter template, not runtime authority.

`ops/check-configurable-workflow.mjs` runs from `check:spec`. It rejects disconnected
backlog/architecture links, missing obligations, fixed-stage substitution, unsupported
task references, false pass without evidence, unsafe receipt paths, unlinked active
packets and completed owner tasks without contribution evidence. Final T-132 also
requires every WF obligation passed. Sixteen negative mutations and one synthetic
positive contribution check test this **structural guard only**. No UI, Temporal,
compiler, BPMN adapter or workflow runtime test is implemented by that checker.

The approved packet must define the exact contribution scope and fixtures. Each
task records its bounded contribution before completion; global integrated WF
acceptance is separate. This avoids requiring a prerequisite schema/install task
to wait on downstream runtime/restore qualification while preventing it from
claiming the complete feature. Real reviewers/evidence authenticity remain outside
what a metadata validator can prove.

## Continuing the original roadmap

Added 24 concrete vectors (120 expected observations) for T-50–T-55:

| Task | New positive, failure, recovery and cross-boundary expectations |
| --- | --- |
| T-50 | Shared service identity across subsidiaries; ancestry/authority conflict; transfer during offboarding; scoped partner projection |
| T-51 | Shared identity/context across lenses; filtered vs denied; outage/reconnect; semantic edit vs layout preference |
| T-52 | Parallel checks/conditional review/bounded rework; invalid recursion/ports/checkpoints; DST and duplicate messages; uncertain compensation |
| T-53 | Three-way independent-field merge; authority/delete conflicts; lost publish response; historical-run-safe revert |
| T-54 | Manual and software realization of one goal; evidence distinct from links; renamed/retired concepts; deployed artifact reverse trace |
| T-55 | Full vs loss-aware projected interchange; quarantined invalid imports; interrupted import replay; extension compatibility/migration |

Cumulative original-scenario vectors: **188/480**, 1,049 expected observations,
59/132 tasks. Remaining **292 scenarios across 73 tasks** without the vector layer.
Separately **0/16 additional WF obligations passed**. All original statuses remain
planned/not_run; no product milestones or independently reviewed packets were added.

All prior handoff rules for actual schema seed bytes, independent observation
adapters, complete fault matrices and real provider/runtime evidence apply. The
T-52 calendar example pins Europe/London's 2026-10-25 overlap: earlier occurrence
01:30 local corresponds to 00:30Z; later 01:30Z must not create a second trigger.
Qualification must record actual tzdata/runtime versions and test both candidates,
not assume a scheduler library makes this business decision. Display labels like
recovery-pending must map explicitly to canonical persisted states in each packet.

## Remaining work and next slice

The next original-scenario slice is T-56–T-67: commitments/economics, workforce and
suppliers, customers/value streams, risk/controls, strategy/simulation, information/
system refinement, physical work, collaboration, readiness and Sentinel ingestion/
claim review. The remaining later portfolio scenarios stay required as well.

The framework decision is specified, not installed or production-qualified.
Temporal adds self-host/runtime-history operations; qualification must cover these
alongside PostgreSQL and brokered effects. Replacing the prior scheduler baseline
requires reviewed adapter/migration slices, not an immediate unapproved stack swap.
No additional external research is needed to continue the listed scenario work.
